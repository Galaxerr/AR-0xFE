[tool.poetry]
name = "fase3"
version = "0.1.0"
description = ""
authors = ["Francesco Papa"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.11"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
